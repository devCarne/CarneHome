<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="stu.kms.carnehome.mapper.ReplyMapper">

    <select id="list" resultType="stu.kms.carnehome.domain.ReplyVO">
        select replyNo, postNo, replyContent, userName, replyDate, updateDate
        from carnehome_reply
        where postNo = #{postNo}
        limit #{pageVO.offset} , #{pageVO.amountPerPage}
    </select>

    <select id="getReply" resultType="stu.kms.carnehome.domain.ReplyVO">
        select * from carnehome_reply where replyNo = #{replyNo}
    </select>

    <select id="getReplyCount" resultType="Long">
        select count(replyNo)
        from carnehome_reply
        where postNo = #{postNo}
    </select>

    <insert id="write">
        insert into carnehome_reply (replyNo, postNo, replyContent, userName) VALUES (nextval(seq_carnehome_reply), #{postNo}, #{replyContent}, #{userName})
    </insert>

    <update id="modify">
        update carnehome_reply set replyContent = #{replyContent}, updateDate = sysdate() where replyNo = #{replyNo}
    </update>

    <delete id="delete">
        delete from carnehome_reply where replyNo = #{replyNo}
    </delete>
</mapper>